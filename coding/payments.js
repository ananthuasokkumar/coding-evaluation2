let promis = new Promise(function (resolve, reject) {
    let cardno = document.getElementById(card);
    let password = document.getElementById(pass);
    if(cardno.value=="masai"&&password.value=="12345")
})
